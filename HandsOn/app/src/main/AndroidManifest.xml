<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.handson">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".SurveyJobValueActivity"/>
        <activity android:name=".SurveyJobAptitudeActivity"/>
        <activity android:name=".StartNewSurveyActivity" />
        <activity android:name=".PastResultActivity" />
        <activity android:name=".MbtiActivity" />
        <activity android:name=".MenuSelect" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".SurveyResultActivity" />
        <activity android:name=".RecommendListActivity" />
        <activity android:name=".SurveySTEMMajorSuitabilityActivity"/>
        <activity android:name=".SurveyJobInterestActivity"/>
        <activity android:name=".SurveyActivity" />
    </application>
</manifest>